let user = null; // This signifies that we have no user object yet
if (user === null) {
    console.log("No user data available.");
}
let result = "a" / 5;
if (isNaN(result)) {
    console.log("The result is not a valid number.");
}

let r = 5/0;
console.log(r);

let users = null;
console.log(isNaN(users));


